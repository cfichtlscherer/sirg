language: python

dist: xenial

python: 
  - 3.7

branches:
  only:
  - master

before_install:
  - pip install networkx
  - pip install codecov
          #  - python --version
          #  - pip install -U pip
          #  - python setup.py install

install:
    - pip install -e .

script:
    - python -m pytest --color=yes
    - coverage run test_generate.py

after_success:
  - codecov
 
